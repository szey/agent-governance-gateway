# 参与贡献

[English](CONTRIBUTING.md) | 简体中文

感谢你帮助 Agent Governance Gateway 变得更实用、更容易验证。

## 开发流程

1. 大型改动前先创建 Issue，说明要验证的安全属性、Bug 或功能；
2. 保持策略决定确定、可解释；
3. 每个新路由、规则或运行时信号都要新增或更新测试；
4. 运行 `gofmt`、`go vet ./...` 和 `go test -race ./...`；
5. 在 Pull Request 中说明改动解决的威胁或失败方式。

除非模型分类是可选信号且不能覆盖确定性拒绝，否则不要把它加入受信任的策略路径。未知身份、能力、资源和 Scope 必须继续默认拒绝。

Discovery 扫描器必须只读、明确限定范围、产生可解释证据，并谨慎处理敏感数据。每个新签名都需要正向、负向、跳过目录和误报测试。资产清单不能保存源文件正文或未脱敏秘密。

## 文档语言规则

每份说明性 Markdown 文档必须有英文和简体中文两个对应版本。中文文档作为产品修改时的工作源版本：中文含义发生变化时，要在同一个 Pull Request 中同步英文版。两个版本的代码标识、状态标签、安全边界、日期、来源链接和能力声明必须一致；翻译不能把规划中的功能写成已经实现。

由调研推动的改动应使用 `$research-to-product` 并遵守 [`.codex/research-to-product.json`](.codex/research-to-product.json)。来源权威度与本项目验证状态必须分开；`V0/V1` 建议不能跳过 synthetic fixture/实验门槛直接写成已实现能力。

## Commit 范围

优先提交小而独立的 Commit，每个 Commit 都应保持 Server 可以运行、测试可以通过。示例和 fixture 中不能包含凭据、生产审计记录或敏感请求数据。
