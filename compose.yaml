services:
  agent-governance-gateway:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - agent-governance-audit:/app/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

volumes:
  agent-governance-audit:
